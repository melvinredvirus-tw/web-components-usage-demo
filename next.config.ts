import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
  images: {
    domains: [
      'placeimg.com',
      'i.imgur.com',
      'cdn.pixabay.com',
      'api.escuelajs.co',
      'images.unsplash.com',
      'placehold.co',
    ],
  },
};

export default nextConfig;
